export type CreateStudentParams = {
    firstName: string;
    lastName: string;
    email: string;
    enrollmentDate: string;
};

export type UpdateStudentParams = {
    firstName: string;
    lastName: string;
    email: string;
    enrollmentDate: string;
};